# Core runtime dependencies for ME_Layer evaluation scripts.
# If your cluster requires a specific CUDA wheel, install torch first and
# then run: python -m pip install -r requirements.txt
torch>=2.6.0
transformers>=4.56.0,<5.0.0
datasets
numpy<2.0.0
pandas
pyarrow>=19.0.0
tqdm
matplotlib
safetensors
sentencepiece
protobuf

# verl / SFT / GRPO training stack.
accelerate
codetiming
dill
hydra-core
omegaconf
peft
pybind11
pylatexenc
ray[default]>=2.41.0
tensordict>=0.8.0,<=0.10.0,!=0.9.0
torchdata
wandb
packaging>=20.0
tensorboard
fastapi
uvicorn
einops
cupy-cuda12x
trl

# Math reward helpers used by verl reward-score modules.
latex2sympy2_extended
math-verify

# Install the vendored verl package used by the SFT/GRPO training scripts.
-e ./verl

# GRPO rollout backend. This matches the hsink environment.
vllm==0.14.0
